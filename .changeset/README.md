# Changesets

Use Changesets for any pull request that changes the published `rmtcdx` package.

Create a changeset:

```bash
npm run changeset
```

Version pending changesets locally:

```bash
npm run version-packages
```

Publish from a trusted environment:

```bash
npm run release:publish
```
